import React from 'react';
import SplashScreenView from './SplashScreenView';

/** Legacy splash route — auth routing is handled at the root navigator. */
const Splash = () => <SplashScreenView />;

export default Splash;
