export interface OfferBanner {
  id: string;
  title: string;
  subtitle?: string;
  badgeLabel?: string;
  discount?: string;
  image?: string;
  expiresAt?: string;
  shopId?: string;
  offerId?: string;
}
